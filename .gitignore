# Dependency directories
/node_modules
/ios/Pods

# Output directories
/android/app/build
/ios/build
/ios/Pods
/ios/DerivedData

# Gradle files
/android/.gradle
/android/local.properties

# Android build cache (optional)
/.gradle
/app/.externalNativeBuild

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
*.temp
*.bak

# macOS specific files
.DS_Store
.AppleDouble
Icon?

# Editor/IDE specific files
.idea
.vscode
*.sublime-*

# Environment files (Optional, if applicable)
.env
.env.local
.env.*.local

# Debug keystore (if not needed in the repository)
/android/app/debug.keystore

# Test coverage reports (if generated locally)
/coverage

# Android virtual device files (if not needed in the repository)
/android
/android/app/*.jks
*.keystore

# Build generated files
*.apk
*.aab
*.apks
*.xcodeproj/*.pbxproj
*.xcodeproj/xcuserdata
*.xcworkspace

# Compiled JavaScript files
**/*.bundle
**/*.map
**/*.jsbundle

# Sourcemap files (Optional, if applicable)
*.sourcemap

# Metro Bundler cache (Optional, if applicable)
/metro-cache
/metro

# Metro Bundler logs (Optional, if applicable)
/logs

# Other sensitive files (e.g., API keys, credentials)
# Add any other sensitive files here

# Ignore npm audit (Optional, if using npm for package management)
.npmrc

# Ignore Yarn integrity file (Optional, if using Yarn for package management)
.yarn-integrity

# React Native CLI log (Optional)
react-native.log

# React Native Debugger log (Optional)
react-native-debugger.log
